# Single nginx config — all traffic proxies to the api-gateway.
# The api-gateway is the sole HTTP entry point; services are pure Kafka consumers.
events { worker_connections 2048; }

http {
    resolver 127.0.0.11 valid=5s;
    server {
        listen 80;
        
        # We use some long timeouts here for long-running requests like batch_init
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
        send_timeout 300s;
        
        location / {
            set $upstream http://api-gateway:8000;
            proxy_pass   $upstream;
            proxy_set_header Host              $host;
            proxy_set_header X-Real-IP         $remote_addr;
            proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        access_log /var/log/nginx/server.access.log;
    }
    access_log /var/log/nginx/access.log;
}